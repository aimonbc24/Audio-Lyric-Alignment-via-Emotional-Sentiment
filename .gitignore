# Env / secrets
.env
.conda/
venv/

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# Raw audio — download locally via scripts/download_data.py; never commit.
# (The mp4s currently in history will be removed in a separate history scrub.)
Dataset/data/mp4/
*.wav

# Large intermediate request/response dumps
Dataset/data/parallel_*.jsonl
